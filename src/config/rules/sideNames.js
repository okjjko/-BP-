/**
 * 阵营显示名默认值（功能1：阵营名称自定义）
 * 负责人：开发者 B
 *
 * road2 / road4 对应「选择了 2 路 / 4 路」的阵营，
 * 与 player1/player2 无固定绑定（由选边动态决定）。
 * 这里的值只影响显示文案，不影响 BP 模板逻辑。
 */
export default {
  road2: '二路',
  road4: '四路'
}
