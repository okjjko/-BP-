# 服务器环境变量（用于 server/index.js）
# Webhook 自动部署密钥（生成方式：openssl rand -hex 32）
# 重要：这是后端密钥，不要加 VITE_ 前缀（VITE_ 会被打进前端 bundle 泄露给访问者）
WEBHOOK_SECRET=your-webhook-secret-here
PORT=8080

# 前端环境变量（真实值放 .env.local，已 gitignore）
VITE_TURN_USERNAME=bpuser
VITE_TURN_CREDENTIAL=your-turn-credential-here
