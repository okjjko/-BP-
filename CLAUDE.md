# CLAUDE.md

本文件是 Claude Code (claude.ai/code) 在本仓库工作时的项目指令入口。

## 项目主指令

完整的项目说明、开发命令、架构、关键设计决策与规则约定统一维护在 **@AGENTS.md**（AI 编码助手通用主文件，本文件通过 `@` 导入自动加载其全部内容）。

请以 `AGENTS.md` 为权威来源——修改架构/约定/规则时同步更新它，**不要**在本文件重复维护同样内容（避免双份漂移）。

## Claude 专属补充

仅记录对 Claude Code 而非所有 agent 适用的指令（当前无）。
